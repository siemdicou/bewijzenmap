// window.addEventListener("load", function () {
// 	var user = {
// 		name:"hi",

// 	}
// 	var arr.forEach(callback[, thisArg])

// })


// Note elision, there is no member at 2 so it isn't visited
var numbers=[2, 5, 9,7,4,54];

numbers.forEach(function(number) {
  console.log(number);
});
// logs:
// a[0] = 2
// a[1] = 5
// a[3] = 9